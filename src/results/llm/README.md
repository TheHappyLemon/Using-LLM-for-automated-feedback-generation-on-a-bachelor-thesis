# Large Language Models responses
Here are the responses generated by LLMs across different scenarios. Each scenario is in a separate directory. All of them have follwing sturcture:
- raw_responses - original unparsed JSONs returned by the API
- responses - model responses. Corresponds to response.json()['message']['content']